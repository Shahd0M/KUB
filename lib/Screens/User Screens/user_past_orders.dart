import 'package:flutter/material.dart';

class UserPastOrdersScreen extends StatelessWidget {
  const UserPastOrdersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("user past orders screen"),
      ),
    );
  }
}
